#include <stdio.h>

typedef enum {false, true}boolean;

int main()
{
    boolean error = false;

    if(error)
    {
        printf("\n there is an error");
    }
    else
    {
         printf("\n there is no error");
    }
  
}